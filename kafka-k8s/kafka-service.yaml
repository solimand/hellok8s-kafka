apiVersion: v1
kind: Service
metadata:
  name: kafka
  labels:
    app: kafka-cluster
spec:
  ports:
  - port: 9092
    name: kafka-port
    protocol: TCP
  selector:
    app: kafka-cluster
    # id: "0"
  type: LoadBalancer